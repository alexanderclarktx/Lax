{
  "name": "@lex",
  "module": "main.ts",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "b": "git checkout main && git pull && git branch -D $(whoami) ; git checkout -b $(whoami)",
    "dev": "bun dev-compile & bun dev-serve",
    "dev-compile": "bun build docs/*.ts --outdir docs --minify --watch",
    "dev-serve": "bun run --hot docs/index.html"
  },
  "dependencies": {
    "typescript": "5.8.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
